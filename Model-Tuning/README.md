## Model Tuning

Techniques for evaluating, tuning, and persisting machine learning models.

## Contents

Grid Search for hyperparameter tuning (Grid_Search.txt). K-Fold Cross Validation (K-FoldCV.txt). Label Encoding and One-Hot Encoding for categorical features (LABEL_ENCODER_ONE_HOT_ENCODER.txt). Min-Max Scaling for feature normalization (MIN_MAX_SCALING.txt). Train-Test Split practice (Train_Test_Split.txt). Saving and loading trained models via Pickle and Joblib (LOAD AND SAVE MODEL VIA PICKLE.txt, SAVE AND LOAD MODEL VIA JOBLIB.txt).
